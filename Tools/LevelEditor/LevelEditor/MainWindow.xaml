<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:LevelEditor"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	x:Class="LevelEditor.MainWindow"
	x:Name="window"
	Title="MainWindow"
	>

    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="Grid" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" />
        <ListBox x:Name="Objects" Grid.Column="1" IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding TileGroups, ElementName=window}"/>
        <local:TileGroup HorizontalAlignment="Left" VerticalAlignment="Top"/>
    </Grid>
</Window>